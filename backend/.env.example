# ParkIt Backend Environment Variables Example
# --- Server Configuration ---
# Port the Express API will listen on
PORT=3000
# --- Database Configuration (Example for PostgreSQL) ---
DB_HOST=localhost
DB_PORT=5432
DB_USER=root
DB_PASSWORD=database_pwd
DB_NAME=parkit_db
# --- Security/Authentication ---
# A long, random string used to sign JWT tokens (should be changed in actual .env)
JWT_SECRET=super_secret_key_change_me_in_production
# --- External Services / IoT Integration ---
# API Key for a third-party service used to calculate traffic/route time
MAPS_API_KEY=your_traffic_maps_api_key_here
# Credentials for real-time notification service (e.g., Firebase, MQTT Broker)
# Example for an MQTT broker (often used for IoT)
MQTT_HOST=mqtt.broker.com
MQTT_PORT=1883
