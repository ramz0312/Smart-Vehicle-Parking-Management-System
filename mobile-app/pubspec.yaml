name: parkit_mobile_app
description: The official mobile application for the ParkIt smart parking system.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^1.1.0           
  provider: ^6.1.1       

  
  flutter_map: ^5.0.0    
  geolocator: ^11.0.0    

  firebase_core: ^2.25.3 
  firebase_messaging: ^14.7.15 
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1  

flutter:
  uses-material-design: true

